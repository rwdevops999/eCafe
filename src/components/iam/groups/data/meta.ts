import { initMetaBase, MetaBase } from "@/data/meta"

export interface Meta extends MetaBase {
}

export const initUserMeta = {
    ...initMetaBase
} 

export const initGrouprMeta = {
    ...initMetaBase
} 
